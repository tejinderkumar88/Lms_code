const config = {
    baseURL: process.env.NEXT_PUBLIC_BASE_URL,
    headers: {
        
        'ngrok-skip-browser-warning': true, // Set the content type for file upload
    },

};

export default config;